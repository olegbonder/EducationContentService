"use client";

export default function LessonPage({ params }: { params: { id: string } }) {
  // const { id } = React.use(params);

  return (
    <div className="container mx-auto py-8">
      <h1 className="text-2xl font-bold">Урок</h1>
    </div>
  );
}
